# ===========================================
# Vercel Environment Variables (PRODUCTION)
# ===========================================
# Add these in: Vercel Dashboard → Project → Settings → Environment Variables
# Select "Production", "Preview", and "Development" environments

# ─────────────────────────────────────────────
# REQUIRED VARIABLES
# ─────────────────────────────────────────────

# MongoDB Atlas Connection String
# Get from: https://cloud.mongodb.com → Connect → Drivers → Node.js
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pregmed?retryWrites=true&w=majority

# JWT Secret for authentication (use a strong random string)
# Generate one: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# ─────────────────────────────────────────────
# OPTIONAL VARIABLES
# ─────────────────────────────────────────────

# Node Environment (Vercel sets this automatically)
NODE_ENV=production

# Frontend URL for CORS (only needed for custom domains)
# FRONTEND_URL=https://pregmed-client.vercel.app/login
