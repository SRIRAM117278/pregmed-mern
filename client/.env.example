# ===========================================
# Client Environment Variables (Vercel)
# ===========================================
# Add these in: Vercel Dashboard → Project → Settings → Environment Variables

# ─────────────────────────────────────────────
# API URL CONFIGURATION
# ─────────────────────────────────────────────

# For VERCEL DEPLOYMENT (frontend + backend on same domain):
# Leave empty or don't set - uses relative /api path automatically
REACT_APP_API_URL=

# For SEPARATE backend deployment (different domain):
# REACT_APP_API_URL=https://your-backend.vercel.app/api

# ─────────────────────────────────────────────
# LOCAL DEVELOPMENT ONLY
# ─────────────────────────────────────────────
# For local dev, create .env.local with:
# REACT_APP_API_URL=http://localhost:5000/api
# PORT=3000
