# Frontend environment variables (React - Create React App)
# Copy this file to .env (for local dev) or configure these
# variables in Netlify UI for your deployed site.

# Backend API base URL
# Local development (when running backend on localhost:5000):
# REACT_APP_API_URL=http://localhost:5000/api

# Production (Render backend): replace with your actual Render service URL
# Example:
# REACT_APP_API_URL=https://your-backend-service.onrender.com/api

# Optional: override React dev server port locally
# PORT=3000
